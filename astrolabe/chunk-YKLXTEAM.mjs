
var DEFAULT_APP_SCHEMA="astrolabe";var LEGACY_APP_SCHEMA="player_insights";var APP_SCHEMA_ENV="PLAYER_INSIGHTS_APP_SCHEMA";var APP_TARGET_ENV="PLAYER_INSIGHTS_TARGET";var LAKEBASE_ENDPOINT_ENV="LAKEBASE_ENDPOINT";function resolveAppSchema(env=process.env){const fromEnv=(env[APP_SCHEMA_ENV]??"").trim();const bundleTarget=(env[APP_TARGET_ENV]??"").trim();const lakebaseBound=Boolean((env[LAKEBASE_ENDPOINT_ENV]??"").trim());if(!bundleTarget&&lakebaseBound&&(!fromEnv||fromEnv===LEGACY_APP_SCHEMA)){return DEFAULT_APP_SCHEMA}if(!fromEnv)return LEGACY_APP_SCHEMA;return fromEnv}var APP_SCHEMA=resolveAppSchema();var POSTGRES_IDENTIFIER=/^[A-Za-z_][A-Za-z0-9_$]*$/;function adoptAppSchema(schema){const candidate=schema.trim();if(!POSTGRES_IDENTIFIER.test(candidate)){throw new Error(`Refusing invalid Postgres schema identifier: ${JSON.stringify(schema)}`)}APP_SCHEMA=candidate;return APP_SCHEMA}function appTable(name){return`${APP_SCHEMA}.${name}`}export{LEGACY_APP_SCHEMA,APP_SCHEMA_ENV,APP_TARGET_ENV,LAKEBASE_ENDPOINT_ENV,APP_SCHEMA,adoptAppSchema,appTable};
