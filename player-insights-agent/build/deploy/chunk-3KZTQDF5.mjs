
function normalizeWorkspaceHost(raw){const trimmed=(raw??"").trim().replace(/\/+$/,"");if(!trimmed)return"";return/^https?:\/\//i.test(trimmed)?trimmed:`https://${trimmed}`}function workspaceAppsUrl(host,workspaceId){const base=normalizeWorkspaceHost(host);if(!base)return"";const org=(workspaceId??"").trim();return org?`${base}/apps-v2?o=${encodeURIComponent(org)}`:`${base}/apps-v2`}function accountConsoleUrlForWorkspace(host){const hostname=(()=>{try{return new URL(normalizeWorkspaceHost(host)).hostname.toLocaleLowerCase()}catch{return""}})();if(hostname.endsWith(".azuredatabricks.net"))return"https://accounts.azuredatabricks.net";if(hostname.endsWith(".gcp.databricks.com"))return"https://accounts.gcp.databricks.com";return"https://accounts.cloud.databricks.com"}function unityCatalogPath(name,part,segment=""){const parts=name.trim().split(".").filter(piece=>piece.length>0);return parts.length===3?`/explore/data/${segment}${parts.map(part).join("/")}`:null}function workspacePath(object){const part=value=>encodeURIComponent(value.trim());switch(object.kind){case"serving-endpoint":return object.name.trim()?`/ml/endpoints/${part(object.name)}`:null;case"genie-space":return object.spaceId.trim()?`/genie/rooms/${part(object.spaceId)}`:null;case"sql-warehouse":return object.warehouseId.trim()?`/sql/warehouses/${part(object.warehouseId)}`:null;case"catalog":return object.catalog.trim()?`/explore/data/${part(object.catalog)}`:null;case"schema":return object.catalog.trim()&&object.schema.trim()?`/explore/data/${part(object.catalog)}/${part(object.schema)}`:null;case"experiment":return object.experimentId.trim()?`/ml/experiments/${part(object.experimentId)}`:null;case"vector-index":return unityCatalogPath(object.index,part);case"table":return unityCatalogPath(object.table,part);case"registered-model":return unityCatalogPath(object.model,part,"models/");case"app":return object.name.trim()?`/apps/${part(object.name)}`:null;case"job":return object.jobId.trim()?`/jobs/${part(object.jobId)}`:null;case"model-version":{const model=unityCatalogPath(object.model,part,"models/");const version=object.version.trim();return model&&version?`${model}/version/${part(version)}`:model}}}function databricksLink(host,object){const base=normalizeWorkspaceHost(host);if(!base)return null;const path=workspacePath(object);return path?`${base}${path}`:null}export{normalizeWorkspaceHost,workspaceAppsUrl,accountConsoleUrlForWorkspace,databricksLink};
