
import{workspaceAppsUrl}from"./chunk-3KZTQDF5.mjs";import"./chunk-A7SHUGSC.mjs";var ACCOUNT_FEEDBACK_GITHUB_URL="https://github.com/smathews13/astrolabe_public/issues/new";var ACCOUNT_FEEDBACK_DEFAULT_SLACK_LABEL="Slack message";var ACCOUNT_FEEDBACK_MAX_LABEL_LENGTH=64;var githubTarget={label:"GitHub issue",url:ACCOUNT_FEEDBACK_GITHUB_URL};function safeSlackTarget(value){if(typeof value!=="string"||!value.trim())return null;try{const parsed=new URL(value.trim());if(parsed.protocol==="slack:"&&parsed.hostname==="user"&&parsed.pathname.replace(/\//g,"")===""&&/^T[A-Z0-9]{8,}$/.test(parsed.searchParams.get("team")??"")&&/^[UW][A-Z0-9]{8,}$/.test(parsed.searchParams.get("id")??"")&&parsed.searchParams.getAll("team").length===1&&parsed.searchParams.getAll("id").length===1&&[...parsed.searchParams.keys()].every(key=>key==="team"||key==="id")){return parsed.toString()}if(parsed.protocol==="https:"&&parsed.hostname.endsWith(".slack.com")&&parsed.hostname!=="hooks.slack.com"&&/^\/team\/[UW][A-Z0-9]{8,}\/?$/.test(parsed.pathname)&&!parsed.username&&!parsed.password&&!parsed.search&&!parsed.hash){return parsed.toString()}if(parsed.protocol==="https:"&&parsed.hostname==="app.slack.com"&&/^\/client\/T[A-Z0-9]{8,}\/search\/?$/.test(parsed.pathname)&&typeof parsed.searchParams.get("q")==="string"&&Boolean(parsed.searchParams.get("q")?.trim())&&(parsed.searchParams.get("q")?.length??0)<=200&&parsed.searchParams.getAll("q").length===1&&[...parsed.searchParams.keys()].every(key=>key==="q")&&!parsed.username&&!parsed.password&&!parsed.hash){return parsed.toString()}}catch{return null}return null}function safeSlackLabel(value){if(typeof value!=="string")return ACCOUNT_FEEDBACK_DEFAULT_SLACK_LABEL;const printable=[...value].map(character=>{const code=character.charCodeAt(0);return code<32||code===127?" ":character}).join("");const plain=printable.replace(/[<>&]/g," ").replace(/\s+/g," ").trim().slice(0,ACCOUNT_FEEDBACK_MAX_LABEL_LENGTH).trim();return plain||ACCOUNT_FEEDBACK_DEFAULT_SLACK_LABEL}function accountFeedbackTargets(slackUrl,slackLabel){const safeUrl=safeSlackTarget(slackUrl);return{github:githubTarget,slack:safeUrl?{label:safeSlackLabel(slackLabel),url:safeUrl}:null}}var ACCOUNT_FEEDBACK_SLACK_URL_ENV="PLAYER_INSIGHTS_FEEDBACK_SLACK_URL";var ACCOUNT_FEEDBACK_SLACK_LABEL_ENV="PLAYER_INSIGHTS_FEEDBACK_SLACK_LABEL";function workspaceAppsHref(env=process.env){return workspaceAppsUrl(env.DATABRICKS_HOST??"",env.DATABRICKS_WORKSPACE_ID)}function accountFeedbackTargetsFromEnv(env=process.env){return accountFeedbackTargets(env[ACCOUNT_FEEDBACK_SLACK_URL_ENV],env[ACCOUNT_FEEDBACK_SLACK_LABEL_ENV])}function setupAccountRoutes(appkit){appkit.server.extend(app=>{app.get("/api/account/feedback-targets",(_req,res)=>{res.setHeader("Cache-Control","private, max-age=300");res.json(accountFeedbackTargetsFromEnv())});app.get("/api/account/apps",(_req,res)=>{const href=workspaceAppsHref();if(!href){res.status(503).json({error:"workspace_apps_unavailable",detail:"This app does not know its Databricks workspace URL, so it cannot open the Apps page."});return}res.redirect(303,href)})})}export{ACCOUNT_FEEDBACK_SLACK_LABEL_ENV,ACCOUNT_FEEDBACK_SLACK_URL_ENV,accountFeedbackTargetsFromEnv,setupAccountRoutes,workspaceAppsHref};
