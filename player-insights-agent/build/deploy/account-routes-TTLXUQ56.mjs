
import{workspaceAppsUrl}from"./chunk-3KZTQDF5.mjs";import"./chunk-A7SHUGSC.mjs";function workspaceAppsHref(env=process.env){return workspaceAppsUrl(env.DATABRICKS_HOST??"",env.DATABRICKS_WORKSPACE_ID)}function setupAccountRoutes(appkit){appkit.server.extend(app=>{app.get("/api/account/apps",(_req,res)=>{const href=workspaceAppsHref();if(!href){res.status(503).json({error:"workspace_apps_unavailable",detail:"This app does not know its Databricks workspace URL, so it cannot open the Apps page."});return}res.redirect(303,href)})})}export{setupAccountRoutes,workspaceAppsHref};
